const defaults = {
  scriptId: 'google-maps-api',
  defer: false,
  async: true,
  body: true,
  key: null,
  libraries: ['places'],
  scriptURL: '//maps.googleapis.com/maps/api/js'
}

module.exports = defaults
